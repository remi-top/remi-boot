## Service
apiVersion: v1
kind: Service
metadata:
  name: dev-remi-boot-web
  labels:
    app: dev-remi-boot-web
spec:
  type: NodePort
  ports:
    - name: dev-remi-boot-web
      port: 1010
      protocol: TCP
      targetPort: 1010
      nodePort: 30010
  selector:
    app: dev-remi-boot-web
---
## Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-remi-boot-web
  labels:
    app: dev-remi-boot-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dev-remi-boot-web
  template:
    metadata:
      labels:
        app: dev-remi-boot-web
    spec:
      containers:
      - image: harbor.******.com/remi-top/dev-remi-boot-web
        imagePullPolicy: Always
        name: dev-remi-boot-web
        ports:
        - containerPort: 1010
          protocol: TCP
        env:
        - name: NACOS_HOST
          value: 192.168.31.200
        - name: NACOS_PORT
          value: "30888"
        - name: NAME_SPACE
          value: dev
        - name: GROUP_NAME
          value: dev
        - name: spring.application.name
          value: remi-sys-web
        - name: spring.profiles.active
          value: dev
        resources:
          limits:
            cpu: "1"
            memory: "500Mi"
          requests:
            cpu: "0.2"
            memory: "200Mi"
        volumeMounts:
        - name: logs
          mountPath: /logs
      volumes:
      - name: logs
        persistentVolumeClaim:
          claimName: remi-log